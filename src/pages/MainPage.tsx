import Layout from "../components/UI/Layout";

export default function MainPage(): React.ReactNode {
  return (
    <Layout>
      <div>
        <button className="btn btn-primary">MainPage</button>
      </div>
    </Layout>
  );
}
